import React from 'react';
import Navbar from '@components/Navbar';

const Header = () => {
  return (
    <header id="header">
      <Navbar />
    </header>
  );
};

export default Header;
